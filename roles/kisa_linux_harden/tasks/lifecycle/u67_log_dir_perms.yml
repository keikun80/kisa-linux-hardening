# =============================================================================
# U-67 로그 디렉터리(/var/log) 소유자 root + 권한 755 이하
# =============================================================================

- import_tasks: "../frags/file_perms.yml"
  vars:
    fp:
      label: "U-67"
      paths: ["/var/log"]
      owner: root
      group: root
      mode: "0640"
      required: true
